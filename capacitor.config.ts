import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.flyingkrakenwag',
  appName: 'flying-kraken-wag',
  webDir: 'dist'
};

export default config;
